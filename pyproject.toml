[project]
name = "suntime-ics-generator"
version = "0.1.0"
description = ""
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "suntime (>=1.2.5, <2.0.0)",
    "ics (>=0.7.2, <1.0.0)"
]

[tool.uv]
dev-dependencies = [
    "mypy (>=1.5.1, <2.0.0)",
    "ruff (>=0.9.6, <1.0.0)"
]
